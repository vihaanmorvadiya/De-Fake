"""Frame and audio extraction interface."""

from pathlib import Path


def extract_frames(video_path: str | Path, sample_rate: float = 2.0) -> list:
    """TODO: Sample decoded video frames at `sample_rate` FPS using FFmpeg."""
    return []
